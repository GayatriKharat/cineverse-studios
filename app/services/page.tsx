import Link from "next/link";
import { services } from "@/lib/site-data";
export default function Services(){return <main className="page-shell"><section className="page-hero wrap"><p className="eyebrow">Services</p><h1>Every stage. <em>One studio.</em></h1><p>Bring us in for a focused assignment or a full creative partnership — each division is designed to work independently or together.</p></section><section className="division-list wrap">{services.map(s=><Link href={`/services/${s.slug}`} key={s.slug} className="division-card" style={{backgroundImage:`linear-gradient(90deg,#080808 15%,#08080850),url(${s.image})`}}><span>{s.number}</span><div><h2>{s.title}</h2><p>{s.strap}</p></div><b>Explore ↗</b></Link>)}</section><section className="coming-soon wrap"><p className="eyebrow">Planned roadmap</p><h2>Talent, IP & <em>Distribution.</em></h2><p>Creator management, talent management, brand collaborations, licensing, streaming and syndication are on the way.</p></section></main>}
