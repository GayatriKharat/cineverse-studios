import { notFound } from "next/navigation";
import Link from "next/link";
import { resourceTypes } from "@/lib/site-data";
import { FaqList } from "@/components/faq-list";
const entries=["How the right production process protects the idea","Making branded content people choose to spend time with","What a clear creative brief unlocks","On building campaigns that travel across platforms"];
const gallery=["film-automotive.png","film-music.png","film-virtual.png","service-brand.png","service-documentary.png","service-drone.png","service-photography.png","service-vfx.png"];
export function generateStaticParams(){return resourceTypes.map(({slug})=>({slug}))}
export default async function ResourcePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const resource=resourceTypes.find(r=>r.slug===slug);if(!resource)notFound();if(slug==="faqs")return <main className="page-shell"><section className="page-hero wrap"><p className="eyebrow">Resources / FAQs</p><h1>Useful answers, <em>upfront.</em></h1></section><section className="faq-section wrap"><FaqList/></section></main>;if(slug==="gallery")return <main className="page-shell"><section className="page-hero wrap"><p className="eyebrow">Resources / Gallery</p><h1>Behind the <em>work.</em></h1></section><section className="gallery-grid wrap">{gallery.map((image,i)=><div key={image} style={{backgroundImage:`url(/${image})`}}><span>Frame 0{i+1}</span></div>)}</section></main>;return <main className="page-shell"><section className="page-hero wrap"><p className="eyebrow">Resources / {resource.title}</p><h1>{resource.title} from<br/>the <em>studio.</em></h1><p>{resource.copy}</p></section><section className="editorial-list wrap">{entries.map((entry,i)=><article key={entry}><span>{slug==="news"?`12.08.2026`: `0${i+1} / Insight`}</span><h2>{entry}</h2><Link href="/contact">Read more ↗</Link></article>)}</section></main>}
